# Optional. Leave unset for local dev (Vite proxies /api to localhost:3001).
# On Vercel, set this to your backend URL (no trailing slash), e.g.:
# VITE_API_URL=https://your-crm-api.railway.app
# VITE_API_URL=https://your-crm-api.onrender.com
